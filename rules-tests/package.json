{
  "name": "splitcruiser-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Executable tests for firestore.rules and storage.rules against the Firebase emulator.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project splitcruiser-rules-test 'vitest run'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.2",
    "firebase-tools": "^13.29.1",
    "vitest": "^2.1.8"
  }
}
