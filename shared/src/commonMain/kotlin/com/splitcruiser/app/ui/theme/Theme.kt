package com.splitcruiser.app.ui.theme

// Platform-agnostic theme configuration
// Actual theme implementation is in platform-specific code (Android Compose, iOS SwiftUI)
object SplitCruiserThemeConfig {
    // Default: platforms should detect system dark mode
    const val DEFAULT_DARK_MODE_ENABLED = false
}
